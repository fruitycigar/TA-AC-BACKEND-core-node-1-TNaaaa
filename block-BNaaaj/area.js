
module.exports = {
    squareArea(side) {
        return side ** 2;
    },
    rectangleArea(length, breadth) {
        return length * breadth;
    },
    circleArea(r) {
        return Math.PI * r * r;
    }
}